// Configurations for allowing access to MySQL DB running on the server.
let conf = {
    host: "localhost",
    user: "root",
    password: "password",
    database: "fluentflex"
};

module.exports = conf;